[project]
name = "qgis-plugin-template"
version = "0.0.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9,<3.10"
dependencies = []

[dependency-groups]
dev = [
    "bandit>=1.8",
    "pyqt5>=5.15.11",
    "pyright>=1.1.408",
    "pytest>=8.4.2",
    "ruff>=0.15.7",
]

[tool.bandit]
exclude_dirs = ["tests", ".venv", ".claude"]

[tool.ruff.lint]
# Pin to Ruff's classic default rule set (pycodestyle errors + Pyflakes).
# Newer Ruff versions enable more rules by default (N/FA/UP...), which would
# otherwise flag the QGIS plugin folder name (N999) and other non-issues.
select = ["E4", "E7", "E9", "F"]